#include "main.h"
/**
 * _abs - entry point
 * Description: check the absolute value of int
 * @n: int to be checked
 * Return: n | -n
 */
int _abs(int n)
{
	if (n >= 0)
	{
		return (n);
	}
	return (-n);
}
